export default function CreateCoursePage() {
  // if (redirect) {
  //   return <Navigate to="/signin" replace={true} state={{ from: location }} />;
  // }

  return (
    <>
      <div>Create Course Outline</div>
    </>
  );
}
